<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<property name="LOGS_FOLDER" value="/var/log/gismo" />
	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<layout class="ch.qos.logback.classic.PatternLayout">
			<Pattern>
				%d{yyyy-MM-dd HH:mm:ss}  %-5level %logger{36}:%M - %msg%n
			</Pattern>
		</layout>
	</appender>
  	<appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    	<file>${LOGS_FOLDER}/confosimoc.log</file>
    	<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
	      	<!-- daily rollover -->
	      	<fileNamePattern>${LOGS_FOLDER}/confosimoc.%d{yyyy-MM-dd}.log</fileNamePattern>
	
	      	<!-- keep 30 days' worth of history -->
	      	<maxHistory>30</maxHistory>
    	</rollingPolicy>

    	<encoder>
      		<pattern>%d{HH:mm:ss.SSS} WEB %-5level %logger{35} - %msg%n</pattern>
    	</encoder>
  	</appender> 

	<logger name="fr.oovinfo" level="debug"
		additivity="false">
		<appender-ref ref="STDOUT" />
		<appender-ref ref="FILE"></appender-ref>
	</logger>

	<root level="info">
		<appender-ref ref="STDOUT" />
		<appender-ref ref="FILE"></appender-ref>
	</root>

</configuration>
